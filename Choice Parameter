
pipeline {
    agent any
      parameters {

        choice(name: 'CHOICE', choices: ['One', 'Two', 'Three'] as String[], description: 'Pick something')
        
        stages {
            stage('Example') {
                steps {               
                    echo "Choice: ${params.CHOICE}"               
            }
        }
    }
        }
