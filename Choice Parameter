
pipeline {
    agent any
      options([parameters([choice(choices: "debug\nrelease\ndev",
        description: '', name: 'CHOICE')]), pipelineTriggers([])])
        
        stages {
            stage('Example') {
                steps {               
                    echo "Choice: ${params.CHOICE}"               
            }
        }
    }
        }
